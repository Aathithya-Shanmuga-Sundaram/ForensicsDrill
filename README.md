# ForensicsDrill-
A safe, sandboxed, interactive CLI tool that generates simulated incident artifacts inside a workspace directory. Students interact with the scenario using a SOC-style console to "investigate" the simulated incident without touching real system processes or network interfaces.
